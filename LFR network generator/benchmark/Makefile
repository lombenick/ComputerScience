.PHONY: clean All

All:
	@echo "----------Building project:[ benchmark - Debug ]----------"
	@cd "benchmark" && "$(MAKE)" -f  "benchmark.mk"
clean:
	@echo "----------Cleaning project:[ benchmark - Debug ]----------"
	@cd "benchmark" && "$(MAKE)" -f  "benchmark.mk" clean
